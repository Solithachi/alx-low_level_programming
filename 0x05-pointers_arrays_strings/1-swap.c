#include "main.h"
#include <stdio.h>

/**
 * void swap_int(int *a, int *b) - integer swap
 *
 * Return: Always 0.
 */
void swap_int(int *a, int *b)
{
	int swap;

	int a;
	swap = *a;
	int b;
	swap = *b;
	*a = *b;
}
