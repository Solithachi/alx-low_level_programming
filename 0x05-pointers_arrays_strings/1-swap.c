#include "main.h"
#include <stdio.h>

/**
 * void swap_int(int *a, int *b) - integer swap
 *
 * Return: Always 0.
 */
void swap_int(int *a, int *b)
{
	int a;
	int b;

	swap = *a;
	*a = *b;
	*b = swap;
}
